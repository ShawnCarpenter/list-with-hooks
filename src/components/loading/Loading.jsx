import React from 'react';
import PropTypes from 'prop-types';

const Loading = props => {
  return (
    <div>
      <h1>Loading</h1>
    </div>
  );
};

Loading.propTypes = {

};

export default Loading;
